name: Deploy RecruitIQ

on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'

      - name: Install Composer dependencies
        run: composer install --no-interaction --prefer-dist

      - name: Install Node dependencies
        run: |
          cd frontend
          npm install
          npm run build

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: 65.254.93.21
          username: act
          password: Blaze4321!
          script: |
            mkdir -p /home/act/public_html/recruitiq.automatedconetruck.com.au
            rsync -avz --exclude '.git' ./ /home/act/public_html/recruitiq.automatedconetruck.com.au
